<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Hnry Tax Calculator" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
